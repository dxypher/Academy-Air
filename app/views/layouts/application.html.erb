<!DOCTYPE html>
<html>
<head>
  <title>Academy Air</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	
	<div id="container">
		<header>
			<h1><%= link_to "Academy Air", root_url %></h1>
			<nav>
				<ul>
					
					
					<%if @user%>
					<li><%= link_to "View My Profile", user_url(@user.id) %></li>
					<li><%= link_to "Sign Out", logout_url %></li>
					<%else%>
						<li><%= link_to "Login", login_url%></li>
						<li><%= link_to "Sign Up", new_user_url%></li>			
					<%end%>
					<li><%= link_to "Help", help_url %></li>
				</ul>
			</nav>
			<%if @user%>
			<p>Hello, <%=@user.name%>!</p>
			<%end%>
		</header> 

		<% if flash[:notice] %>
  <p style="color: white; background: green; padding: 4px">
  <%= flash[:notice] %>
  </p>
<% end %>

		<div id="content">
			<%= yield %>
		</div>
		
		<footer>
			&copy;2012 Academy Air, Inc.  Got comments or suggestions? Keep 'em to yourself!
		</footer>
	</div>
</body>
</html>
